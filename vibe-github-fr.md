# Vibe Mobile Repo Usage Rules
<img width="10500" height="4620" alt="Frame 18" src="https://github.com/user-attachments/assets/a8609eac-836c-423a-9201-2d4c73ca568f" />



این سند قوانین نام‌گذاری برنچ و نحوه استفاده از مخزن موبایل Vibe را تعریف می‌کند. همه اعضای تیم از این استاندارد پیروی می‌کنند. هدف این است که هر کسی به مخزن نگاه می‌کند، با **یک نگاه** بفهمد هر برنچ چه کاری انجام می‌دهد.

---

## قانون کلی

در نام برنچ **نام شخص نوشته نمی‌شود.** نام کسی که برنچ را push می‌کند به‌صورت خودکار در Git و GitHub نمایش داده می‌شود، بنابراین نیازی به نام نیست. نام برنچ به سؤال «چه کسی» پاسخ نمی‌دهد، بلکه به سؤال **«این برنچ چه کاری انجام می‌دهد»** پاسخ می‌دهد.

**فرمت:**

```
tip/kisa-aciklama
```

- همیشه **حروف کوچک**
- بین کلمات از **خط تیره** (`-`) استفاده شود
- توضیح **۲ تا ۴ کلمه**، کوتاه و واضح باشد

---

## انواع برنچ و معانی آن‌ها

### `feature/` — قابلیت جدید

زمانی استفاده می‌شود که یک قابلیت جدید به اپلیکیشن اضافه می‌شود. صفحه جدید، سیستم جدید، یک جریان (flow) جدید و غیره.

از روی `main` ساخته می‌شود و پس از اتمام کار، با PR دوباره به `main` مرج می‌شود.

```
feature/live-gift-lottie
feature/media-editor-screen
feature/new-profile-redesign
```

### `fix/` — رفع باگ عادی

برای رفع باگ‌های معمولی و غیراضطراری استفاده می‌شود. وضعیتی نیست که production را خراب کند؛ در جریان عادی از روی `main` پیش می‌رود.

از روی `main` ساخته می‌شود و با PR به `main` بازمی‌گردد.

```
fix/player-next-track-bug
fix/filter-slider-behavior
fix/bottomsheet-layout
```

### `hotfix/` — باگ اضطراری در production

برای باگی بحرانی که در نسخه منتشرشده روی کاربران تأثیر می‌گذارد و قابل انتظار کشیدن نیست، استفاده می‌شود.

با جریان عادی **تفاوت دارد:** از روی `main` ساخته نمی‌شود، بلکه از روی **تگ نسخه‌ای که در حال انتشار است** ساخته می‌شود. رفع باگ هم به استور فرستاده می‌شود و هم دوباره به `main` مرج می‌شود تا اصلاح از بین نرود.

```
hotfix/iap-trial-display
hotfix/livescreen-crash
hotfix/payment-verification
```

### `release/` — نسخه فریزشده برای ارسال به استور

برنچی است که نسخه‌ای که قرار است به استور (Play Store / App Store) فرستاده شود، در آن فریز می‌شود. پس از ساخته شدن این برنچ، **قابلیت جدید وارد آن نمی‌شود**، فقط آخرین رفع باگ‌های مربوط به همان نسخه وارد می‌شوند.

این امکان را می‌دهد که در حین انتظار برای بررسی استور (review)، روی `main` کار نسخه بعدی ادامه پیدا کند. پس از انتشار نسخه، کامیت با `tag` علامت‌گذاری می‌شود (`v1.4.0`).

```
release/1.4.0
release/1.5.0
```

### `chore/` — کارهای غیرمرتبط با کد

برای کارهای نگهداری که رفتار اپلیکیشن را تغییر نمی‌دهند استفاده می‌شود. به‌روزرسانی پکیج، تغییر config، ارتقای نسخه Flutter، مرتب‌سازی فایل‌ها و غیره.

از روی `main` ساخته می‌شود و با PR به `main` بازمی‌گردد.

```
chore/flutter-3-27-upgrade
chore/update-dependencies
chore/ci-config
```

---

## جدول خلاصه

| نوع | برای چه کاری | از کجا ساخته می‌شود | به کجا مرج می‌شود |
|-----|-------------|---------------------|-------------------|
| `feature/` | قابلیت جدید | `main` | `main` |
| `fix/` | رفع باگ عادی | `main` | `main` |
| `hotfix/` | باگ اضطراری production | تگ نسخه منتشرشده | `main` + استور |
| `release/` | فریز نسخه | `main` | تگ می‌شود |
| `chore/` | کارهای نگهداری غیرکدی | `main` | `main` |

---

## جریان کاری (خلاصه)

1. `main` همیشه پایدار و قابل deploy است.
2. برای هر کار جدید، با نوع مناسب یک برنچ ساخته می‌شود (`feature/...`، `fix/...` و غیره).
3. پس از اتمام کار، یک Pull Request (PR) باز می‌شود.
4. PR از review عبور می‌کند و اگر CI سبز باشد به `main` مرج می‌شود.
5. برنچ‌ها کوتاه‌عمر نگه داشته می‌شوند — بیش از ۲ تا ۳ روز نگه داشته نمی‌شوند تا conflict انباشته نشود.

---

## یادآوری‌ها

- مستقیماً به `main` **push انجام نمی‌شود**، همه چیز از طریق PR عبور می‌کند.
- هرچه برنچ کوچک‌تر و متمرکزتر باشد، review آسان‌تر می‌شود.
- لیست انواع برنچ ثابت است — برای جلوگیری از شلوغی، از این ۵ نوع خارج نمی‌شویم.
